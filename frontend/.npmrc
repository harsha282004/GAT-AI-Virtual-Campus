# pannellum-react's peer dependency range doesn't yet include React 19,
# even though the package itself works fine with it (verified: no findDOMNode,
# legacy context, or string refs in its source). Without this, `npm install`
# fails with an ERESOLVE peer-conflict error.
legacy-peer-deps=true
