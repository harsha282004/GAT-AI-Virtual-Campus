# Scripts

Operational and development scripts, organized by concern. No scripts are implemented yet — placeholders only.

- `setup/` — environment bootstrap scripts (venv creation, dependency install, Ollama model pull).
- `db/` — database seed/maintenance scripts.
- `ai/` — knowledge-base build scripts, load-testing scripts, and other AI-pipeline utilities.
