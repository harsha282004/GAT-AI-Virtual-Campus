# Tests

- `backend/` — pytest suite for the FastAPI backend (test paths configured in root `pyproject.toml`).
- `frontend/` — frontend test suite (test runner to be selected: Jest or Vitest).
- `e2e/` — end-to-end tests across the full stack (tool to be selected: Playwright or Cypress).

No test files exist yet — this is skeleton-only scaffolding.
