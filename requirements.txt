# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.32

# --- Data validation & settings ---
pydantic>=2.9
pydantic-settings>=2.5

# --- Database ---
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9

# --- AI / RAG stack ---
langchain>=0.3
langchain-community>=0.3
langchain-ollama>=0.2
chromadb>=0.5
ollama>=0.3
# sentence-transformers: local embedding model (all-MiniLM-L6-v2) for the
# knowledge base pipeline (scripts/ai/) — no existing dependency serves this.
sentence-transformers>=3.0

# --- Knowledge base data acquisition (scripts/ai/) ---
# beautifulsoup4: HTML parsing/cleaning for the official GAT website
# collector — no existing dependency parses HTML.
beautifulsoup4>=4.12
# pypdf: text + per-page extraction for official GAT PDFs — no existing
# dependency reads PDFs.
pypdf>=5.0
# rank_bm25: lexical BM25 keyword retrieval for the Phase 2 hybrid retriever
# (scripts/ai/hybrid_retrieval.py) — no existing dependency does term-based
# ranking; ChromaDB only provides dense/semantic search.
rank_bm25>=0.2
# scikit-learn: sklearn.svm.SVR reranking infrastructure for the Phase 3
# reranker (scripts/ai/reranker.py). Already present transitively (pulled in
# by sentence-transformers), but is now imported directly by application
# code, so it needs to be declared explicitly rather than relied on as a
# transitive dependency.
scikit-learn>=1.5

# --- Utilities ---
python-dotenv>=1.0
httpx>=0.27

# --- Media processing (scripts/media/build_panoramas.py resizes/compresses
# real 360 source photos for the tour — not used by the running application) ---
Pillow>=11.0

# --- Testing & tooling ---
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.6
black>=24.8
mypy>=1.11
